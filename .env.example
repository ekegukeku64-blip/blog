# 认证与评论 API 地址（Cloudflare Pages 项目 blog-api）
# 这个值是公开的（会进客户端包），但必须在构建时注入：
#   本地放在 .env；CI 里配一个仓库变量 PUBLIC_API_BASE，由三条 Pages 工作流传给 npm run build。
# 用首次部署后 wrangler 打印的地址，例如 https://blog-api.pages.dev ，将来换自定义域名也改这里。
PUBLIC_API_BASE=https://blog-api.pages.dev

# 旧的 Firebase 六个键已不再被任何代码读取；本地 .env 里保留为注释以便回滚。

# 技术日报友链地址（Footer / 友链页使用）
PUBLIC_TECH_DAILY_URL=https://ekegukeku64-blip.github.io/tech-daily/
