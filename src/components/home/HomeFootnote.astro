<section class="home-footnote">
  <div class="home-footnote-divider"></div>
  <div class="home-footnote-text">
    <span class="font-display font-bold">墨迹</span>
    <span class="mx-3 opacity-20">·</span>
    <span>不端着，先做出来。</span>
  </div>
</section>

<style>
.home-footnote {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 2rem 5rem;
  text-align: center;
}

.home-footnote-divider {
  width: min(18rem, 70vw);
  height: 1px;
  margin: 0 auto 2rem;
  background: linear-gradient(90deg, transparent, rgba(122,149,175,0.36), rgba(200,182,142,0.28), transparent);
  box-shadow: 0 0 22px rgba(122,149,175,0.12);
}

.home-footnote-text {
  display: inline-flex;
  align-items: center;
  padding: 0.75rem 1.15rem;
  border: 1px solid rgba(122,149,175,0.10);
  border-radius: 999px;
  background: rgba(7,12,18,0.28);
  font-size: 0.82rem;
  color: rgba(168, 175, 186, 0.62);
  font-family: var(--font-serif);
}

@media (max-width: 768px) {
  .home-footnote { padding: 0 1.25rem 3.5rem; }
}
</style>
